# pipeline:
#   test:
#     image: golang
#     commands:
#       - go get
#       - go test

# services:
#   database:
#     image: postgres
#     environment:
#       - POSTGRES_USER=postgres
#       - POSTGRES_DB=test

pipeline:
  Run Something:
    image: busybox
    commands:
      - echo 'hello'

services:
  postgres:
    image: postgres
    # environment:
      # - 'POSTGRES_USER: pguser'
    #   - POSTGRES_PASSWORD=pgpass
    # ports:
    #   - '15432:5432'


